# State: self-evolving-agent — PRD/Spec/Design review & alignment

**Date:** 2026-07-24
**Initiative:** self-evolving-agent
**Phase:** design review + alignment (pre-implementation)
**Status:** ⏸ PAUSED — design coherent, impl not started, awaiting Guild feasibility spike

---

## Goal (this session)

Advocate-review whether the PRD + spec can deliver daily self-evolution; fix doc issues; align the design to the chosen backend.

---

## What was done

### Review
- Advocate review of PRD + spec + design + impl-plan, backed by direct codebase verification + 3 subagents (doc reconciliation, impl-plan feasibility, Claude Code hook ground-truth).
- Archived: `docs/self-evolving-agent/devil-advocate/2026-07-24-prd-spec-review/report.md`

### Key finding & decision
- **Platform vs Application:** PRD = the self-evolution *engine* (platform); design/spec/impl-plan = a QA-agent *skill* (one application). The engine is **0% built**.
- **Chose Path 1b:** QA skill is self-contained; backend = **Guild OSS** (+ Jam.dev MCP + slimmed SQLite `knowledge_index`), per PRD Appendix A's v2 guidance. (Appendix A did *not* reject Guild — it endorsed it as a v2 FTS5 alternative.)
- Decision doc: `docs/self-evolving-agent/dependency-and-sequencing.md` (Path 1a / 1b / 2 tradeoffs).

### Doc fixes applied
- **6 broken cross-references:** fabricated `PRD §5.7` (3 spots: spec + impl-plan×2) → `Global CLAUDE.md §0.5`; design `§2.1.2`→`§2.2.2`; Core Loop `§2.1`→`§2.2`; spec §2.2 MEMORY.md overclaim; deleted orphan stubs `docs/initiatives/self-evolving-agent/{plan,spec}.md`; en PRD duplicate `§1.4 Knowledge Taxonomy`→`§1.6`.
- **design aligned to Guild:** added "v2 Backend: Guild OSS + Jam.dev MCP (Path 1b)" section; rewrote §1.1 (three-surface diagram), §6 #1; swept all remaining FTS5/two-tier refs → Guild Lore. design ↔ impl-plan now consistent on Guild.

---

## Current state
| Artifact | Status |
|---|---|
| design | ✅ Guild-aligned, coherent |
| spec | ⚠️ NOT aligned to Guild (still FTS5: QA_E003/004 @ L411/412, §6.3 @ L490) |
| impl-plan | ⚠️ Guild-aware (`mcp__guild__*` TODOs) but 100% stub |
| Guild + Jam | ❌ NOT installed (`guild not found`, `JAM_API_KEY` unset) |

---

## NOT done — feasibility risks still open (by severity)

1. 🔴 **Guild uninstalled/unvalidated** — Path 1b's entire foundation. No smoke-test of install / `guild init` / MCP registration / quest-lore lifecycle / hybrid search.
2. 🔴 **Execution-model ambiguity** — is `qa-orchestrator` a Python pipeline (must shell out to `guild` CLI) or a Claude Code agent skill (calls `mcp__guild__*`)? impl-plan mixes both. **Must resolve before coding.**
3. 🟡 **impl 100% stub** — all `mcp__guild__*` are TODO; orchestrator breaks after 1 iter; execute_fix/dynamic_check/discovery are placeholders.
4. 🟡 **spec not aligned to Guild** (3 FTS5 spots).
5. 🟡 **auto-fix safety** design-only — `execute_fix` stub, no sandbox/rollback.
6. 🟡 **Layer 3 dynamic test generation** — research-grade; stubbed (`return None`).
7. 🟡 **git-push hook** — matcher unvalidated + body is `echo`.
8. 🟡 **weak-model JSON robustness** — no try/except.
9. 🟡 **no budget cap** on DeepSeek calls.

---

## ▶ Next step (resume here)

**Guild feasibility spike (impl-plan Task 0):** install Guild + Jam → register MCP server → define Oaths → smoke-test one quest → lore → brief lifecycle.
- If Guild works → continue Path 1b: align spec to Guild, resolve the execution-model question, then wire the 7 skills' real logic.
- If Guild doesn't → fall back to **Path 1a** (reuse existing `analytics.db` + `knowledge.py` DeepSeek client; zero new deps).

---

## Separate workstream (out of Path 1b — don't lose track)

- **Self-evolution ENGINE** (`memory_store` / FTS5 / sync / curator / lifecycle / post-session summarization) = **0% built**. This is the actual "越来越好用" body. Path 1b only ships a QA tool, not evolution.
- **PRD hook architecture** has 5 technical errors to fix before relying on the engine: `SessionStop`→`SessionEnd`; missing `SubagentStop`; missing `async:true`; `$SESSION_ID` (arrives via stdin JSON, not env); PRD wrongly says PostToolUse misses MCP/Skill (it doesn't — only Agent/subagent).

---

## Artifacts produced this session
- `docs/self-evolving-agent/devil-advocate/2026-07-24-prd-spec-review/report.md` (new)
- `docs/self-evolving-agent/dependency-and-sequencing.md` (new)
- `design/qa-autonomous-agent-design.md` — Guild section + §1.1/§6 alignment + FTS5 sweep
- `spec/qa-autonomous-agent-spec.md` — §5.7, §2.2 fixes
- `impl-plan/qa-autonomous-agent-impl-plan.md` — §5.7 fixes (×2)
- `prd/self-evolving-agent-prd.md` — §1.4→§1.6
- deleted: `docs/initiatives/self-evolving-agent/{plan,spec}.md`

> ⚠️ **All changes uncommitted.** `design/`, `spec/`, `impl-plan/` dirs are still git-untracked. Commit pending explicit request.
