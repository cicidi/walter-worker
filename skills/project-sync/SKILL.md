---
name: project-sync
version: 0.1.0
description: Re-inject project catalog into IDE config files
triggers:
- project-sync
when-to-use: When user needs to re-inject project catalog into ide config files
license: MIT
compatibility: claude-code,opencode,gemini
user-invocable: true
---
# project-sync

Re-inject project catalog into IDE config files.

## Usage

```bash
coworker project sync
```
